/** @core public surface: the kernel contracts (audio runtime + kernel facts). */
export * from "./kernel";
/** Root runtime facade and non-context audio analysis service. */
export * from "./application";
