import type {IManageable, IManager, IUseableManager} from "./types";
import Manager from "./manager";
import UseableManager from "./useable-manager";

export type {
    IManageable,
    IManager,
    IUseableManager
}
export {
    Manager,
    UseableManager,
}

export default Manager
