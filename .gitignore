# —— IDE / 编辑器 —— #
.idea
.vscode

# —— Agent / 工具链（本地，不入库） —— #
.agents
.claude
skills-lock.json

# —— 构建产物 —— #
# Wails v3 输出到仓库根的 bin/,并把 go-task 的状态放在 .task/。
# 前置 / 锚定到仓库根,避免顺带忽略任何嵌套的同名目录。
/bin
/.task
/build/darwin/icons.icns
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe
frontend/dist
frontend/coverage
frontend/.tanstack
frontend/package.json.md5

# —— 依赖 —— #
node_modules
frontend/node_modules

# —— 本地环境变量（可能含 provider 凭证） —— #
.env
.env.local
.env.*.local

# —— OS 噪音 —— #
.DS_Store
